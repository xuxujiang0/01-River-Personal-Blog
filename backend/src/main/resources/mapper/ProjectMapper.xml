<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.river.blog.mapper.ProjectMapper">
    
    <resultMap id="BaseResultMap" type="com.river.blog.entity.Project">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="image" property="image"/>
        <result column="link" property="link"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <sql id="Base_Column_List">
        id, user_id, title, description, image, link, sort_order, created_at, updated_at
    </sql>
    
    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM projects
        WHERE id = #{id}
    </select>
    
    <!-- 查询所有项目 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM projects
        ORDER BY sort_order ASC, created_at DESC
    </select>
    
    <!-- 插入项目 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO projects (user_id, title, description, image, link, sort_order)
        VALUES (#{userId}, #{title}, #{description}, #{image}, #{link}, #{sortOrder})
    </insert>
    
    <!-- 更新项目 -->
    <update id="update">
        UPDATE projects
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="image != null">image = #{image},</if>
            <if test="link != null">link = #{link},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 删除项目 -->
    <delete id="deleteById">
        DELETE FROM projects WHERE id = #{id}
    </delete>
    
    <!-- 查询项目的技术栈 -->
    <select id="selectTechStackByProjectId" resultType="string">
        SELECT tech_name FROM project_tech_stack WHERE project_id = #{projectId}
    </select>
    
    <!-- 插入项目技术栈 -->
    <insert id="insertTechStack">
        INSERT INTO project_tech_stack (project_id, tech_name) VALUES (#{projectId}, #{techName})
    </insert>
    
    <!-- 删除项目技术栈 -->
    <delete id="deleteTechStack">
        DELETE FROM project_tech_stack WHERE project_id = #{projectId}
    </delete>
    
</mapper>
